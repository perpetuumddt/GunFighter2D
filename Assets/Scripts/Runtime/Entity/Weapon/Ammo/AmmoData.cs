using UnityEngine;

namespace Gunfighter.Runtime.Entity.Weapon.Ammo
{
    public class AmmoData : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
