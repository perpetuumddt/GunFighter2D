using UnityEngine;

namespace Gunfighter.Runtime.Entity.Weapon.MeleeWeapon
{
    public class WeaponMelee : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
